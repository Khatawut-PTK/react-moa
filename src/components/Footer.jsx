import React from "react";
import { Flex, Typography, Layout } from "antd";

const { Title } = Typography;
const { AppFooter } = Layout;

const Footer = () => {
  return (
    <>
      <AppFooter>123445</AppFooter>
    </>
  );
};

export default Footer;
